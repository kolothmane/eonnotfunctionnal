{
  "name": "eon-local-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local static server for eon.battlecell.io",
  "scripts": {
    "start": "node server.js",
    "start:host": "cross-env HOST=0.0.0.0 node server.js",
    "start:port": "node -e \"process.env.PORT=process.argv[1]; require('./server');\" 8080"
  },
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
